# MUI Theme Lab

Interactive React + MUI design-system playground with:

- Maia-inspired neutral preset
- live semantic color pickers
- radius + light/dark controls
- rich MUI component showcase
- component-level MUI theme overrides
- Vite build suitable for GitHub Pages

## Run

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```
